# generator-sb

**This is niche. Probably don't use it.**

```bash
npm install -g yo
npm install -g generator-sb
```

Create a pattern from the root of the project. Patterns will be placed in `./src/templates/partials/<pattern-name>/`
```bash
yo sb:pattern
```
